spring:
  application:
    name: profile-service
  datasource:
      url: "jdbc:mysql://localhost:3306/sphere_user_profile"
      driverClassName: "com.mysql.cj.jdbc.Driver"
      username: root
      password: root
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
server:
  port: 8081
  servlet:
    context-path: /profile-service

